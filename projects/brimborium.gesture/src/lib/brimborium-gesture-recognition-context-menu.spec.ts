import { BrimboriumGestureRecognitionContextMenu } from './brimborium-gesture-recognition-context-menu';

describe('BrimboriumGestureRecognitionContextMenu', () => {
  it('should create an instance', () => {
    expect(new BrimboriumGestureRecognitionContextMenu()).toBeTruthy();
  });
});
