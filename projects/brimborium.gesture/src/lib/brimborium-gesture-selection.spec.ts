import { BrimboriumGestureSelection } from './brimborium-gesture-selection';

describe('BrimboriumGestureSelection', () => {
  it('should create an instance', () => {
    expect(new BrimboriumGestureSelection()).toBeTruthy();
  });
});
