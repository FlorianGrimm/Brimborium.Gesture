import { BrimboriumGestureHandle } from './brimborium-gesture-handle';

describe('BrimboriumGestureHandle', () => {
  // it('should create an instance', () => {
  //   const directive = new BrimboriumGestureHandle();
  //   expect(directive).toBeTruthy();
  // });
  it('TODO',()=>{});
});
