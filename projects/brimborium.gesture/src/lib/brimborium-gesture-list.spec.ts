import { BrimboriumGestureList } from './brimborium-gesture-list';

describe('BrimboriumGestureList', () => {
  // it('should create an instance', () => {
  //   const directive = new BrimboriumGestureList();
  //   expect(directive).toBeTruthy();
  // });
  it('TODO',()=>{});
});
