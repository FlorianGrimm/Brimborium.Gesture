import { BrimboriumGestureInteractionEffectContextMenu } from './brimborium-gesture-interaction-effect-context-menu';

describe('BrimboriumGestureInteractionEffectContextMenu', () => {
  it('should create an instance', () => {
    expect(new BrimboriumGestureInteractionEffectContextMenu()).toBeTruthy();
  });
});
