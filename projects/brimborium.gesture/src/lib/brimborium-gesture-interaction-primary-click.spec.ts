import { BrimboriumGestureInteractionPrimaryClick } from './brimborium-gesture-interaction-primary-click';

describe('BrimboriumGestureInteractionPrimaryClick', () => {
  it('should create an instance', () => {
    expect(new BrimboriumGestureInteractionPrimaryClick()).toBeTruthy();
  });
});
