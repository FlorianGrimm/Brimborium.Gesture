import { IBrimboriumGestureRecognition } from "./brimborium-gesture-consts";
import  { BrimboriumGesturePhase } from "./brimborium-gesture-phase";

export class BrimboriumGestureDefinition {
    getListGestureRecognition(): IBrimboriumGestureRecognition[] {
        return [];
    }
    getListStartPhase(): BrimboriumGesturePhase[] {
        return [];
    }
}
