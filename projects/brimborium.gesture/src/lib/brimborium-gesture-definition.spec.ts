import { BrimboriumGestureDefinition } from './brimborium-gesture-definition';

describe('BrimboriumGestureDefinition', () => {
  it('should create an instance', () => {
    expect(new BrimboriumGestureDefinition()).toBeTruthy();
  });
});
