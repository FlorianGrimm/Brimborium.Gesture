import { BrimboriumGesture } from './brimborium-gesture';

describe('BrimboriumGesture', () => {
  // it('should create an instance', () => {
  //   const directive = new BrimboriumGesture();
  //   expect(directive).toBeTruthy();
  // });
  it('TODO',()=>{});
});
