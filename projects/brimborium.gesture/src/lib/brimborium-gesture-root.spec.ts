import { BrimboriumGestureRoot } from './brimborium-gesture-root';

describe('BrimboriumGestureRoot', () => {
  // it('should create an instance', () => {
  //   const directive = new BrimboriumGestureRoot();
  //   expect(directive).toBeTruthy();
  // });
  it('TODO',()=>{});
});
