import { BrimboriumGestureOptions } from './brimborium-gesture-options';

describe('BrimboriumGestureOptions', () => {
  it('should create an instance', () => {
    expect(new BrimboriumGestureOptions()).toBeTruthy();
  });
});
