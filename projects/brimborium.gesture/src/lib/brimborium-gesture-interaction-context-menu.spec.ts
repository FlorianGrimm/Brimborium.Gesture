import { BrimboriumGestureInteractionContextMenu } from './brimborium-gesture-interaction-context-menu';

describe('BrimboriumGestureInteractionContextMenu', () => {
  it('should create an instance', () => {
    expect(new BrimboriumGestureInteractionContextMenu()).toBeTruthy();
  });
});
